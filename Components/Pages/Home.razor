@page "/"
@using TodoApp.Helpers
@using TodoApp.Models
@using TodoApp.Models.DataModels

<h1>Hello, world!</h1>

Welcome to your new app.
<button class="btn btn-primary" @onclick="TestDatabase">Click me</button>

@code {

    private void TestDatabase()
    {
        var allRecords =  DatabaseHelper.GetData<TodoCreationRecord>();
        var newRecord = DatabaseHelper.UpsertDataWithReturn(new TodoCreationRecord()
        {
            Active = true,
            Description = "test"
        }).FirstOrDefault();
        newRecord.Description = "test2";
        DatabaseHelper.UpsertData(newRecord);
        var updatedRecord = DatabaseHelper.SelectData<TodoCreationRecord>(newRecord.Id);

    }
}
